# install npm and node along with modules

npm install -g npm@latest # ensure latest version of npm is installed
npm install # assuming this run from the frontend folder, install from package.json
rm install.sh
echo "Hope this works! Use 'npm start' from the frontend folder to start."